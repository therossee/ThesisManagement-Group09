const APPLICATION_STATUS = Object.freeze({
    WAITING_FOR_APPROVAL: 'waiting for approval',
    REJECTED: 'rejected',
    ACCEPTED: 'accepted',
    CANCELLED: 'cancelled'
});

module.exports = {
    APPLICATION_STATUS
};
